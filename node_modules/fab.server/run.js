/////////
// RUN //
/////////
module.exports = function ( write ) {
 
  write = write || function x(){ return x }
  
  return function read( obj ) {
    if ( obj && obj.apply ) {
      var fn = obj;
      arguments[ 0 ] = write;
      return fab.run( fn.apply( undefined, arguments ) );
    }
    write = write.apply( undefined, arguments );
    return read;
  }
}//END RUN
